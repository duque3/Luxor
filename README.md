# Para instalar el bot se nesecita los siguientes conmandos :

```bash
> pkg install git
> pkg install nodejs
> pkg install ffmpeg
> pkg install imagemagick
> git clone https://github.com/duque3/Luxor.git
> cd Luxor
> ls
> bash install.sh
> npm install
> npm start
